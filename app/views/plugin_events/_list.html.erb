<% 	current_events = PluginEvent.for_item(item).current.happening_first.approved %> 
<%= link_to icon("calendar") + " #{current_events.size} #{t("single.upcoming")} #{PluginEvent.model_name.human(:count => current_events.size)}", {:action => "view", :controller => "items", :id => item.id, :anchor => "#{PluginEvent.model_name.human(:count => :other)}"} if current_events.size > 0 %>	

